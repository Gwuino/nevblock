# НЕВБЛОК — сайт ООО «НЕВБЛОК»

Сайт-визитка и каталог продукции (газосиликатные блоки, ФБС, кольца ЖБИ, шлакоблок, полублок, крышки и днища ЖБИ, услуги манипулятора) с админ-панелью Django для редактирования товаров и цен.

## 🚀 Технологии

- **Frontend**: Next.js 15, TypeScript, Tailwind CSS
- **Backend**: Django 5, Django REST Framework
- **База данных**: PostgreSQL
- **Контейнеризация**: Docker, Docker Compose
- **Веб-сервер**: Nginx

## 📋 Возможности

- ✅ Каталог продукции с категориями
- ✅ Калькулятор блоков для расчета количества материалов
- ✅ Админ-панель Django для управления товарами и категориями
- ✅ REST API для интеграции
- ✅ Автоматический деплой через Git webhook
- ✅ Responsive дизайн для мобильных устройств

## 🛠️ Локальная разработка

### Требования

- Node.js 20+
- Python 3.11+
- Docker и Docker Compose (опционально)

### Запуск через Docker (рекомендуется)

```bash
docker-compose up -d
```

Приложение будет доступно:
- Frontend: http://localhost:3001
- Django API: http://localhost:8000
- Django Admin: http://localhost:8000/admin

### Запуск без Docker

#### Frontend (Next.js)

```bash
npm install
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000).

#### Backend (Django)

```bash
cd backend
pip install -r requirements.txt
python manage.py migrate
python manage.py import_data
python manage.py createsuperuser
python manage.py runserver
```

## 🔐 Админ-панель Django

- Адрес: http://localhost:8000/admin
- По умолчанию создается пользователь: `admin` / `admin`
- ⚠️ **Обязательно измените пароль после первого входа!**

## 📦 Деплой на сервер

### Быстрый старт

```bash
bash setup-server.sh
```

Этот скрипт автоматически установит Docker, загрузит проект и запустит приложение.

### Настройка автоматического деплоя

1. Создайте webhook в настройках репозитория GitHub/GitLab:
   - URL: `http://ваш-сервер/webhook`
   - Secret: значение `WEBHOOK_SECRET` из `.env.prod` на сервере
   - Events: только push события

2. При каждом `git push` приложение автоматически обновится!

Подробная документация: см. [DEPLOY.md](./DEPLOY.md) и [QUICK_START.md](./QUICK_START.md)

## 📁 Структура проекта

```
nevblock/
├── app/                    # Next.js приложение
│   ├── admin/              # Админ-панель (Next.js)
│   ├── api/                # API routes
│   └── page.tsx            # Главная страница
├── backend/                # Django приложение
│   ├── catalog/            # Django app для каталога
│   └── nevblock/            # Django settings
├── components/              # React компоненты
├── lib/                     # Утилиты и типы
├── data/                    # JSON данные (для импорта)
├── nginx/                   # Конфигурация Nginx
├── docker-compose.yml       # Docker Compose для разработки
├── docker-compose.prod.yml  # Docker Compose для production
└── deploy.sh                # Скрипт деплоя
```

## 🔧 API Endpoints

### Django REST API

- `GET /api/categories/` - Список категорий
- `GET /api/products/` - Список товаров
- `GET /api/products/?category=key` - Товары по категории
- `POST /api/products/` - Создать товар (требует аутентификации)
- `PUT /api/products/{id}/` - Обновить товар
- `DELETE /api/products/{id}/` - Удалить товар

### Next.js API Routes

- `GET /api/products` - Список товаров
- `POST /api/products` - Создать товар
- `PUT /api/products/[id]` - Обновить товар
- `DELETE /api/products/[id]` - Удалить товар
- `GET /api/categories` - Список категорий
- `PUT /api/categories` - Обновить категории

## 📝 Лицензия

Проект разработан для ООО «НЕВБЛОК»

## 🔗 Ссылки

- Репозиторий: https://github.com/Gwuino/nevblock
- Production: http://45.80.130.183
